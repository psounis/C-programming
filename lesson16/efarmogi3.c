#include <stdio.h>

main()
{
	char string[100];
	char c;
	int i,check;
	
	printf("Dwse ti simvoloseira: ");
	scanf("%s",string);
	while (string[0]!='\n')
	{
		scanf("%s",string);
		
		printf("%s",check);
	}
	string[i]='\0';
	
	printf("H symvoloseira einai: ");
	i=0;
	while (string[i]!='\0')
	{
		putchar(string[i]);
		
		i++;
	}
	
}
